import Main from './main-scene';
import Loading from './loading-scene';
import Title from './title-scene';
import Result from './result-scene';

export default [Loading, Main, Title, Result];
export { Main, Loading, Title, Result };
