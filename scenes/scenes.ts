import Main from './main-scene';
import Title from './title-scene';
import Result from './result-scene';
import Rule from './rule-scene';

export default [Title, Rule, Main, Result];
export { Main, Rule, Title, Result };
